# AI Scrum Master - Система управления совещаниями с ИИ

## 🎯 Обзор проекта

AI Scrum Master — это комплексная система для автоматизации управления совещаниями, которая использует искусственный интеллект для анализа разговоров, генерации сводок и создания задач. Система состоит из нескольких взаимосвязанных компонентов, обеспечивающих полный цикл управления совещаниями от присоединения к звонку до отслеживания выполнения задач.

## 🏗️ Архитектура системы

```
┌─────────────────────┐    ┌──────────────────────┐    ┌─────────────────────┐
│   Telegram Bot      │    │   FastAPI Backend    │    │  Selenium Agent     │
│   (Интерфейс)      │◄──►│   (Основная логика)  │◄──►│  (Google Meet)      │
└─────────────────────┘    └──────────────────────┘    └─────────────────────┘
           ▲                          │                           │
           │                          ▼                           │
┌─────────────────────┐    ┌──────────────────────┐              │
│  Telegram Mini App  │    │   OpenRouter AI      │              │
│   (Dashboard)       │    │   (Анализ текста)    │              │
└─────────────────────┘    └──────────────────────┘              │
                                      │                           │
                                      ▼                           ▼
                           ┌──────────────────────┐    ┌─────────────────────┐
                           │   База данных        │    │   Caption Parser    │
                           │   (Сессии/задачи)   │    │   (Извлечение речи) │
                           └──────────────────────┘    └─────────────────────┘
```

## 📦 Компоненты системы

### 1. 🤖 Telegram Bot (`bot.py`)
**Назначение**: Основной интерфейс взаимодействия с пользователями

**Функции**:
- Запуск и остановка AI-агентов для совещаний
- Отображение статуса агента в реальном времени
- Получение и отправка сводок совещаний
- Интеграция с Mock Jira для создания задач
- Просмотр истории совещаний

**Возможности**:
- Валидация ссылок Google Meet
- Фоновое отслеживание статуса (каждые 5 секунд)
- Передача вопросов от AI-агента пользователю
- Автоматическое создание задач из сводки

### 2. 🖥️ FastAPI Backend (`main.py`)
**Назначение**: Центральная система управления и обработки данных

**API Endpoints**:
```
GET  /                           # Информация о системе
GET  /sessions                   # Список всех сессий
POST /start_agent               # Запуск нового агента
POST /stop_agent/{session_id}   # Остановка агента и генерация сводки
GET  /agent_status/{session_id} # Получение статуса агента
POST /api/transcript/chunk      # Обработка частей транскрипта
POST /api/transcript/final      # Финальная обработка и отправка в Telegram
GET  /api/session/{session_id}/history # История сессии
DELETE /api/session/{session_id}       # Удаление сессии
```

**Интеграции**:
- **OpenRouter AI**: Анализ текста и генерация сводок
- **Telegram API**: Отправка уведомлений
- **Selenium Agent**: Управление браузерными агентами
- **Logging**: Подробное логирование в `ai.log`

### 3. 🌐 Selenium Agent (`bot_service.py`)
**Назначение**: Автоматизация взаимодействия с Google Meet

**Функции**:
- Автоматическое присоединение к совещаниям Google Meet
- Включение субтитров и их парсинг в реальном времени
- Извлечение имен участников и структурирование диалогов
- Отправка частей транскрипта на backend каждые 5 минут

**Алгоритм работы**:
1. **Инициализация**: Запуск Chrome с настройками для медиа-доступа
2. **Вход в совещание**: Автоматический ввод имени "AI-Agent"
3. **Активация субтитров**: Включение через горячую клавишу 'c'
4. **Парсинг**: Извлечение текста субтитров с определением спикеров
5. **Отправка данных**: Регулярная передача данных на backend

**Селекторы для парсинга субтитров**:
```javascript
"[jsname='YSxPC']"           // Основной селектор субтитров
".a4cQT"                     // Альтернативный класс
"[data-caption-text]"        // Атрибут данных
"*[aria-live='polite']"      // ARIA live region
".zs7s8d.jxFHg"             // Новый UI Meet
```

### 4. 📱 Telegram Mini App (Dashboard)
**Назначение**: Веб-интерфейс для управления задачами и просмотра аналитики

**Функции**:
- **Дашборд задач**: Отображение статусов (Completed, In Progress, Pending)
- **История совещаний**: Просмотр всех проведенных совещаний
- **Сводки**: Полные тексты сгенерированных AI сводок
- **Аналитика**: Статистика по участникам, темам, решениям

**Интерфейс**:
- Адаптивный дизайн для мобильных устройств
- Интеграция с Telegram WebApp API
- Реальновременные обновления статусов
- Фильтрация и поиск по задачам/совещаниям

### 5. 🧠 AI Система (OpenRouter Integration)
**Назначение**: Интеллектуальный анализ и обработка содержимого совещаний

**Возможности**:
- **Анализ контекста**: Понимание тематики и важности обсуждений
- **Извлечение участников**: Автоматическое определение участников
- **Генерация задач**: Создание actionable задач из обсуждений
- **Создание сводок**: Структурированные отчеты по совещаниям
- **Определение решений**: Выделение принятых решений

**Модель**: `tngtech/deepseek-r1t-chimera:free`

**Структура сводки**:
```
УЧАСТНИКИ: [список участников]
КЛЮЧЕВЫЕ_РЕШЕНИЯ: [принятые решения]
ЗАДАЧИ_И_ДЕЙСТВИЯ: [список задач с ответственными]
ВОПРОСЫ_ОБСУЖДЕННЫЕ: [основные темы]
ОБЩАЯ_СВОДКА: [краткое резюме]
```

## 🚀 Установка и настройка

### Системные требования
- Python 3.9+
- Google Chrome браузер
- Доступ к интернету для AI API

### 1. Клонирование и установка зависимостей

```bash
# Клонирование репозитория
git clone <repository-url>
cd MeetAgent

# Создание виртуального окружения
python -m venv bot_env
bot_env\Scripts\activate  # Windows
# source bot_env/bin/activate  # Linux/Mac

# Установка зависимостей
pip install -r requirements.txt
```

### 2. Настройка переменных окружения

Создайте файл `.env` в корневой папке:

```env
# Telegram Bot
TELEGRAM_BOT_TOKEN="your_telegram_bot_token"

# OpenRouter AI
OPENROUTER_TOKEN="your_openrouter_token"

# Backend URL (опционально)
BACKEND_URL="http://localhost:8001"
```

### 3. Получение токенов

#### Telegram Bot Token:
1. Напишите [@BotFather](https://t.me/botfather) в Telegram
2. Выполните команду `/newbot`
3. Следуйте инструкциям для создания бота
4. Скопируйте полученный токен

#### OpenRouter Token:
1. Зарегистрируйтесь на [OpenRouter.ai](https://openrouter.ai)
2. Перейдите в раздел API Keys
3. Создайте новый ключ
4. Скопируйте токен

### 4. Запуск системы

```bash
# Терминал 1: Запуск Backend
python main.py

# Терминал 2: Запуск Telegram Bot
python bot.py
```

## 📖 Использование системы

### Для пользователей Telegram Bot

#### 1. Запуск совещания
```
/start → "📞 Начать созвон" → [отправить ссылку Google Meet]
```

#### 2. Мониторинг процесса
- 🟡 "Агент запускает браузер..."
- 🟠 "Агент ждет разрешения на вход..."
- 🟢 "Агент успешно присоединился к звонку!"

#### 3. Завершение и получение результатов
```
"🛑 Завершить созвон" → [получение AI-сводки] → "🚀 Создать задачи в Jira"
```

### Для разработчиков API

#### Запуск агента
```bash
curl -X POST "http://localhost:8001/start_agent" \
  -H "Content-Type: application/json" \
  -d '{
    "meeting_url": "https://meet.google.com/abc-defg-hij",
    "participants_info": null
  }'
```

#### Проверка статуса
```bash
curl "http://localhost:8001/agent_status/{session_id}"
```

#### Остановка агента
```bash
curl -X POST "http://localhost:8001/stop_agent/{session_id}"
```

## 📊 Структура данных

### Модель сессии
```json
{
  "session_id": "uuid",
  "meeting_url": "https://meet.google.com/...",
  "status": "active|stopped|error",
  "created_at": "2025-11-28T12:00:00",
  "participants": ["user1", "user2"],
  "transcript_chunks": ["chunk1", "chunk2"],
  "final_summary": "AI-generated summary",
  "tasks": [
    {
      "id": "task_1",
      "title": "Task title",
      "assignee": "user1",
      "status": "pending|in_progress|completed"
    }
  ]
}
```

### Модель задачи
```json
{
  "task_id": "uuid",
  "session_id": "parent_session_uuid",
  "title": "Task description",
  "assignee": "username",
  "status": "pending",
  "created_at": "2025-11-28T12:00:00",
  "due_date": null,
  "priority": "medium"
}
```

## 🔧 Конфигурация

### Backend настройки (`main.py`)
```python
# AI модель для анализа
AI_MODEL = "tngtech/deepseek-r1t-chimera:free"

# Таймауты
OPENROUTER_TIMEOUT = 150  # секунд для AI обработки
HTTP_TIMEOUT = 30         # секунд для HTTP запросов

# Интервалы обработки
CHUNK_PROCESSING_INTERVAL = 300  # 5 минут
STATUS_POLLING_INTERVAL = 5      # 5 секунд
```

### Selenium настройки (`bot_service.py`)
```python
# Chrome опции
options.add_argument("--use-fake-ui-for-media-stream")
options.add_argument("--disable-web-security")
options.add_argument("--allow-running-insecure-content")

# Разрешения для медиа
prefs = {
    "profile.default_content_setting_values": {
        "media_stream_mic": 1,
        "media_stream_camera": 1
    }
}
```

## 📋 Логирование и мониторинг

### Файлы логов
- `app.log` - общие логи приложения
- `ai.log` - логи AI обработки (UTF-8)
- Console output - runtime статус

### Уровни логирования
```python
logger.info()    # Основные события
logger.warning() # Предупреждения
logger.error()   # Ошибки
logger.debug()   # Отладочная информация
```

### Мониторинг метрик
- Время обработки AI запросов
- Количество обработанных чанков
- Статус подключений к Google Meet
- Успешность отправки Telegram уведомлений

## ⚠️ Устранение неполадок

### Частые проблемы

#### 1. Агент не может присоединиться к совещанию
**Причина**: Ограничения Google Meet или неправильная ссылка
**Решение**: 
- Убедитесь, что ссылка содержит `meet.google.com`
- Проверьте настройки безопасности совещания
- Обновите Chrome до последней версии

#### 2. Субтитры не обнаруживаются
**Причина**: Изменения в UI Google Meet
**Решение**:
- Проверьте логи селекторов субтитров
- Обновите селекторы в `_parse_current_captions()`
- Включите субтитры вручную в браузере

#### 3. AI не генерирует сводки
**Причина**: Проблемы с OpenRouter API или недостаток данных
**Решение**:
- Проверьте токен OpenRouter в `.env`
- Убедитесь в наличии транскрипта
- Увеличьте таймаут AI обработки

#### 4. Telegram Bot не отвечает
**Причина**: Неверный токен или конфликт экземпляров
**Решение**:
- Проверьте `TELEGRAM_BOT_TOKEN` в `.env`
- Остановите все экземпляры бота
- Перезапустите с новым токеном

### Отладка

#### Включение детального логирования
```python
logging.basicConfig(level=logging.DEBUG)
```

#### Проверка состояния компонентов
```bash
# Backend health check
curl http://localhost:8001/

# Проверка активных сессий
curl http://localhost:8001/sessions

# Telegram Bot статус
python -c "import bot; print('Bot configured successfully')"
```

## 🛠️ Разработка и расширение

### Добавление новых AI моделей
1. Добавьте конфигурацию модели в `main.py`
2. Обновите промпты в функциях AI обработки
3. Протестируйте на тестовых данных

### Расширение парсинга субтитров
1. Добавьте новые селекторы в `caption_selectors`
2. Реализуйте специфическую логику извлечения
3. Добавьте fallback методы для надежности

### Интеграция с внешними системами
- Jira API для реального создания задач
- Slack/Teams для уведомлений
- Database для персистентного хранения
- Analytics для метрик и отчетов

## 📈 Производительность

### Оптимизация
- Async/await для HTTP запросов
- Connection pooling для HTTP клиентов
- Batch обработка транскриптов
- Кеширование AI ответов

### Масштабирование
- Горизонтальное масштабирование backend
- Load balancing для множественных агентов
- Database clustering для больших объемов
- CDN для статических ресурсов Mini App

## 🔐 Безопасность

### Защита токенов
- Использование `.env` файлов
- Ограничение доступа к файлам конфигурации
- Регулярная ротация API ключей

### Сетевая безопасность
- HTTPS для production
- Rate limiting на API endpoints
- Input validation для всех параметров
- CORS настройки для Mini App

## 📄 Лицензия

Этот проект разработан для внутреннего использования. Все права защищены.

## 👥 Поддержка

Для получения поддержки:
1. Проверьте раздел "Устранение неполадок"
2. Изучите логи в `app.log` и `ai.log`
3. Создайте issue с подробным описанием проблемы

---

**AI Scrum Master** - Ваш надежный помощник в управлении совещаниями с использованием искусственного интеллекта! 🚀